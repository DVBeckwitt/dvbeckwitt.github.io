# Compiled documents

This folder is the local destination for ignored compiled PDFs. Regenerate after source edits with:

```bash
python scripts/build_cv.py
```

The AI-readable CV is in `../../exports/` and should be used for AI parsing instead of PDFs. Do not commit compiled PDFs unless a future verification check scans and approves them.
